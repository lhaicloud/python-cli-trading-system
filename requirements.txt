# LQ-MTF Strategy — Python Dependencies
# Python 3.11+

# CLI
typer[all]==0.9.0
click==8.1.7   # typer 0.9 is incompatible with click >= 8.2
rich==13.7.1

# Data & Math
pandas==2.2.2
numpy==1.26.4

# HTTP
httpx==0.27.0
websockets==12.0   # PriceMonitor live stream (REST fallback if unavailable)

# Database
SQLAlchemy==2.0.30

# ML
scikit-learn==1.6.1   # must match the version the joblib model dumps were trained with
joblib==1.4.2

# Config / Validation
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1

# Utilities
python-dateutil==2.9.0
pytz==2024.1
